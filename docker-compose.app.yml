services:
  app:
    build: .
    command: uv run gunicorn --worker-class gevent --workers 4 --bind 0.0.0.0:5000 run:app
    env_file:
      - .env
    ports:
      - "5000-5009:5000" 
    networks:
      - hackathon_network
    restart: always

networks:
  hackathon_network:
    external: true