# true for local dev; use false in Docker when benchmarking (lower latency).
FLASK_DEBUG=true
DATABASE_NAME=hackathon_db
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

DEVELOPEMENT_ENV=true

# Docker only: published port for direct access to gunicorn (avoid 5000 on Mac / AirPlay)
HOST_APP_PORT=5001

# Optional: alias cache (Docker Compose sets redis://redis:6379/0 on app)
REDIS_URL=redis://localhost:6379/0
SHORT_LINK_CACHE_TTL=3600

# Docker / gunicorn: raise if k6 still sees >5% errors (default in image 48)
# GUNICORN_WORKERS=64

# k6: k6 run k6/load.js  (defaults to http://127.0.0.1:8080 = nginx LB + Redis check in setup)
# Direct gunicorn only: K6_DIRECT_APP=1 BASE_URL=http://127.0.0.1:5001 k6 run k6/load.js