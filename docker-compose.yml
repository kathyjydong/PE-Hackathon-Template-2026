services:
  db:
    image: postgres:alpine
    environment:
      - POSTGRES_DB=hackathon_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    networks:
      - hackathon_network
    restart: always

networks:
  hackathon_network:
    external: true